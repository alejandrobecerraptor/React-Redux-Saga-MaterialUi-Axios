import React from 'react';
import { Container, Typography, Card, Grid , TextField, Button } from '@material-ui/core';

export default () => {
    return(
        <Container>
            Moviedetail
        </Container>
    );
}